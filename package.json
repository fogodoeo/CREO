{
  "name": "creo-band-oauth",
  "version": "1.0.0",
  "private": true,
  "description": "CREO landing page and NAVER BAND OAuth bridge",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check band-oauth.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
